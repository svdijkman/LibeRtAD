% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ir-compiler.R
\name{ad_ir}
\alias{ad_ir}
\title{Compile mathematical model code to a serializable intermediate representation}
\usage{
ad_ir(code, inputs = character(), outputs = NULL)
}
\arguments{
\item{code}{Character model code. The established LibeRation assignment,
`THETA(i)`, `ETA(i)`, `SIGMA(i)`, and `ERR(i)` syntax is supported.}

\item{inputs}{Optional external input names. Undeclared symbols are appended
as inputs in first-use order.}

\item{outputs}{Optional assignment names to expose. The default exposes all
assignments, with the most recent value for reassigned names.}
}
\value{
A serializable `libertad_ir` object.
}
\description{
Compile mathematical model code to a serializable intermediate representation
}
\examples{
ir <- ad_ir(
  "CL = THETA(1) * exp(ETA(1))\nY = log(CL)",
  outputs = "Y"
)
ir
}
