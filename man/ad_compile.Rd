% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ad-model.R
\name{ad_compile}
\alias{ad_compile}
\title{Compile an R-like mathematical model}
\usage{
ad_compile(
  code,
  inputs = character(),
  outputs = NULL,
  at = NULL,
  wrt = names(at),
  optimize = TRUE
)
}
\arguments{
\item{code}{Character model code. The established LibeRation assignment,
`THETA(i)`, `ETA(i)`, `SIGMA(i)`, and `ERR(i)` syntax is supported.}

\item{inputs}{Optional external input names. Undeclared symbols are appended
as inputs in first-use order.}

\item{outputs}{Optional assignment names to expose. The default exposes all
assignments, with the most recent value for reassigned names.}

\item{at}{Optional named recording point. When supplied, a persistent tape
is recorded immediately.}

\item{wrt}{Inputs with respect to which derivatives are required.}

\item{optimize}{Whether CppAD should optimize the recorded tape.}
}
\value{
An `ADModel` R6 object.
}
\description{
Compile an R-like mathematical model
}
\examples{
model <- ad_compile(
  "CL = THETA(1) * exp(ETA(1))\nPENALTY = log(CL)^2",
  at = c(THETA_1 = 2, ETA_1 = 0),
  wrt = c("THETA_1", "ETA_1"),
  outputs = "PENALTY"
)
model$value_gradient(c(THETA_1 = 2, ETA_1 = 0))
model$hessian(c(THETA_1 = 2, ETA_1 = 0))
}
