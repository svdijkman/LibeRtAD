% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differentiate.R
\name{backdiff}
\alias{backdiff}
\alias{backdiffR}
\alias{backdiffCPP}
\title{Reverse-mode automatic differentiation}
\usage{
backdiff(
  f,
  ...,
  at = NULL,
  backend = c("R", "cpp"),
  record_tape = TRUE,
  print_result = FALSE
)

backdiffR(f, ..., at = NULL, print_result = FALSE)

backdiffCPP(f, ..., at = NULL, print_result = FALSE)
}
\arguments{
\item{f}{A function of one or more numeric arguments.}

\item{...}{Named numeric values at which to evaluate and differentiate.
Values may be scalars or numeric vectors.}

\item{at}{Optional named list of parameter values. Use either \code{...} or
\code{at}, not both.}

\item{backend}{Either \code{"R"} or \code{"cpp"} for the backward pass.}

\item{record_tape}{Logical; record intermediate nodes on the tape.}

\item{print_result}{Logical; print a summary of the result.}
}
\value{
A list with components \code{value}, \code{gradient}, \code{partials},
\code{partials_flat}, and \code{node}.
}
\description{
Differentiates a scalar function with respect to the supplied parameter
values. Parameters are automatically converted to internal AD variables.
Vector parameters are supported; each element receives its own partial
derivative in \code{partials}.
}
\examples{
f <- function(x, y, z) {
  3 * x^4 + y / z / x + z * 3
}
backdiff(f, x = 2, y = 2, z = 3)
f <- function(x) x^2
backdiffR(f, x = 2)
f <- function(x) x^2
backdiffCPP(f, x = 2)
}
